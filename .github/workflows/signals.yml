const { execSync } = require("child_process");

const DISCORD_WEBHOOK_URL = "ТУК_ТВОЯ_WEBHOOK_URL";

const payload = JSON.stringify({
  content: `📊 **Crypto Signal Test**

BTC
Вход: 100 000 USD
Изход 1: 105 000 USD (+5%)
Изход 2: 107 000 USD (+7%)
Стоп: 99 000 USD (-1%)

⏱ GitHub Actions test`
});

execSync(`curl -X POST -H "Content-Type: application/json" -d '${payload}' ${DISCORD_WEBHOOK_URL}`);

console.log("Signal sent to Discord");
